# Manus 调研任务：数据知识产权估值引擎 v1.3/v1.4 系数拆分方案

## 背景

我们正在开发一个「数据知识产权协商估值引擎」（开源项目 github.com/guolliu1033/data-ip-valuation-engine），基于《德阳市数据知识产权协商估值指南（试行）》（德市监函〔2025〕42号）的成本法协商估值方法。

当前核心公式：A = a + (d+e)×b + a×c

- a = 审定直接投入成本（数据购买 + 劳务 + 设备）
- d+e = 加工必要成本（设备 + 劳务）
- b = 加工难度系数（当前仅靠"运算流程数 + 判定条件数"粗分低/中/高三档）
- c = 应用场景价值系数（当前仅靠"好/很好/特好"三档）

德阳指南原文没有提供更细致的系数维度，我们需要参考国标和行业标准，把 b 和 c 拆分成多维子系数。

## 已调研到的参考框架

### 1. GB/T 46353-2025《信息技术 大数据 数据资产价值评估》（2026-05-01 实施）
- 成本法：P = C × δ（δ = 价值调整系数，含质量要素调整）
- 市场法：P = Σ(Qi × Xi1 × Xi2 × Xi3 × Xi4 × Xi5)
  - Xi1 质量调整系数：准确性/一致性/完整性/规范性/时效性/可访问性
  - Xi2 供求调整系数：市场规模/稀缺性/价值密度
  - Xi3 期日调整系数、Xi4 容量调整系数、Xi5 其他调整系数
- 数据质量 6+1 维度：准确性、一致性、完整性、规范性、时效性、可访问性（+开放维度）

### 2. 资产评估专家指引第 9 号——数据资产评估（中评协）
- 成本法：P = TC × (1+R) × U
- 数据效用：U = α × β × (1+l) × (1-r)
  - α 数据质量系数：准确性/完整性/一致性/时效性/唯一性/规范性
  - β 数据流通系数：法律合规性/隐私保护/流通渠道/标准化/权属清晰度
  - l 数据垄断系数：稀缺性/获取难度/市场集中度/不可替代性
  - r 数据价值实现风险系数：技术风险/市场风险/法律风险/安全风险/经营风险/政策风险

### 3. GB/T 46353-2025 价值影响要素
- 生成要素：规划投入/建设投入/运维投入/其他投入；生命周期管理；数据安全合规
- 应用要素：使用范围/应用场景/商业模式/供求关系/数据关联

## 调研任务

请深入调研以下问题，输出一份结构化的技术方案文档：

### 任务 1：v1.3 加工难度系数 b 的子系数拆分方案

1. **子系数维度确定**：参考国标质量维度（α 数据质量系数）和流通维度（β 数据流通系数），结合德阳指南已有的"运算流程数 + 判定条件数"，设计 b 的子系数维度（建议 4-6 个子系数）。每个子系数需要有明确的数据资产评估理论依据。

2. **各子系数分档标准**：每个子系数设计 3-5 个等级，给出每级的量化判定标准（可量化、可操作，不要纯定性描述）。

3. **权重分配方案**：给出各子系数的推荐权重，说明依据。提供至少 2 种权重方案供对比（如等权 vs 专家加权）。

4. **合成公式**：b 如何由子系数合成（加权平均 vs 加权求和 vs 乘法合成），给出推荐公式。

5. **与德阳指南的兼容性**：拆分后的 b 要与现有 A = a + (d+e)×b + a×c 公式兼容，不能改变 L1 公式结构，只是在 L2 层把 b 的推荐逻辑精细化。

### 任务 2：v1.4 应用场景价值系数 c 的子系数拆分方案

1. **子系数维度确定**：参考国标 Xi2 供求调整系数（市场规模/稀缺性/价值密度）、l 数据垄断系数（稀缺性/获取难度/不可替代性）、r 价值实现风险系数的反向，结合德阳指南已有的"好/很好/特好"三档，设计 c 的子系数维度（建议 4-6 个子系数）。

2. **各子系数分档标准**：同上，每级量化判定标准。

3. **权重分配方案**：同上，至少 2 种方案。

4. **合成公式**：同上。

5. **可比案例库**：调研国内数据交易所（上海/北京/深圳/德阳/湖南/昆明等）已成交的数据产品/数据知识产权交易案例，整理 5-10 个可作为 c 系数参照的案例（含交易价格、行业、数据类型、应用场景、稀缺性等关键信息）。

### 任务 3：AI 评估路径设计

1. **v1.3 AI 路径**：用户输入数据加工描述文本，AI 分析复杂度并输出 b 的推荐区间 + 推理链 + 引用依据。设计 prompt 模板和输出格式。

2. **v1.4 AI 路径**：用户输入数据资产画像（行业/规模/质量/合规/可比交易），AI 输出 c 的推荐区间 + 推理链 + 引用政策/案例。设计 prompt 模板和输出格式。

3. **规则 vs AI 双路径切换逻辑**：何时用规则（无网/无额度），何时用 AI（非标/复杂场景）。

### 任务 4：与德阳估值指南的兼容性验证

1. 用省人民医院 × 扬子江（98.8 万）真实案例验证：a=26, d=20, e=6, b=1.4, c=1.4, 收益法=97 万
2. 用拆分后的 b 子系数重新计算，验证能否复现 b=1.4
3. 用拆分后的 c 子系数重新计算，验证能否复现 c=1.4
4. 如果不能精确复现，说明差异原因和可接受范围

## 输出格式要求

请输出一份完整的 Markdown 文档，包含：
1. v1.3 系数 b 拆分方案（含子系数表、分档标准表、权重表、合成公式）
2. v1.4 系数 c 拆分方案（同上 + 可比案例库表）
3. AI 评估 prompt 模板（v1.3 和 v1.4 各一套）
4. 真实案例兼容性验证结果
5. 参考文献/标准清单

文档需可直接用于代码实现，每个子系数的分档标准必须是可编程的 if-else 或数值区间判断。
